# 📋 Sistema de Cadastro de Pessoas (Vanilla JS + JQuery)

Projeto simples de frontend para cadastro e gerenciamento de dados de pessoas, utilizando **JavaScript Vanilla** (com módulos ES6) para a lógica principal, **JQuery** para manipulação do DOM e **LocalStorage** para persistência dos dados.

## ✨ Funcionalidades

* **Cadastro, Edição e Exclusão** de pessoas.
* **Persistência de Dados** via LocalStorage (os dados ficam salvos no seu navegador).
* **Alternância de Tema** (Modo Claro/Modo Escuro) com salvamento da preferência.
* **Validação básica** dos campos do formulário.
* **Suporte a Módulos ES6** (`<script type="module">`) em ambiente de servidor (ex: Live Server do VS Code) e *fallback* para execução direta (`file://`).

## 🛠️ Tecnologias Utilizadas

* **HTML5:** Estrutura da aplicação.
* **CSS3:** Estilização responsiva e temas (claro/escuro).
* **JavaScript (ES6 Modules):** Lógica de classes (`Pessoa`, `StorageManager`) e manipulação de eventos.
* **JQuery:** Utilizado para simplificar a manipulação do DOM e eventos.

## 🚀 Como Executar Localmente

1.  **Clone o repositório** para sua máquina local.
    ```bash
    git clone URL_DO_SEU_REPOSITORIO
    cd sistema-cadastro-pessoas
    ```
2.  **Abra o `index.html`** no seu navegador.

> **Nota:** Para a funcionalidade completa (uso dos módulos ES6), é recomendado abrir o projeto usando uma extensão como **Live Server** no VS Code ou qualquer servidor HTTP local.

## 📄 Licença

Este projeto está licenciado sob a Licença MIT.
